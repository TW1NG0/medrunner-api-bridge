"use strict";
/**
 * MuteController - Adds mute/unmute capability
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.MuteController = MuteController;
const EventLimiter_1 = require("../core/EventLimiter");
const types_1 = require("../core/types");
function MuteController(Base) {
    return class extends Base {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        constructor(...args) {
            super(...args);
            this.muteCallback = null;
            this.muteEventLimiter = new EventLimiter_1.EventLimiter(types_1.EVENT_LIMIT_STATE);
            this.registerRequestHandler(this.handleMuteRequest.bind(this));
        }
        onMute(callback) {
            this.muteCallback = callback;
        }
        async sendMuteEvent(mute, cause = types_1.PHYSICAL_INTERACTION) {
            if (this.muteEventLimiter.isLimited()) {
                return false;
            }
            return this.sendEvent('setMute', { mute }, cause);
        }
        async handleMuteRequest(request) {
            if (request.action !== 'setMute' || !this.muteCallback) {
                return false;
            }
            const mute = request.requestValue.mute;
            const result = await this.muteCallback(this.getDeviceId(), mute);
            // Handle both boolean and object return types
            let success;
            if (typeof result === 'boolean') {
                success = result;
            }
            else {
                success = result.success;
                if (result.message) {
                    request.errorMessage = result.message;
                }
            }
            if (success) {
                request.responseValue.mute = mute;
            }
            return success;
        }
    };
}
//# sourceMappingURL=MuteController.js.map