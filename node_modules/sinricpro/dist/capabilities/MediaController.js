"use strict";
/**
 * MediaController - Adds media playback control capability
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.MediaController = MediaController;
const EventLimiter_1 = require("../core/EventLimiter");
const types_1 = require("../core/types");
function MediaController(Base) {
    return class extends Base {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        constructor(...args) {
            super(...args);
            this.mediaControlCallback = null;
            this.mediaEventLimiter = new EventLimiter_1.EventLimiter(types_1.EVENT_LIMIT_STATE);
            this.registerRequestHandler(this.handleMediaControlRequest.bind(this));
        }
        onMediaControl(callback) {
            this.mediaControlCallback = callback;
        }
        async sendMediaControlEvent(control, cause = types_1.PHYSICAL_INTERACTION) {
            if (this.mediaEventLimiter.isLimited()) {
                return false;
            }
            return this.sendEvent('mediaControl', { control }, cause);
        }
        async handleMediaControlRequest(request) {
            if (request.action !== 'mediaControl' || !this.mediaControlCallback) {
                return false;
            }
            const control = request.requestValue.control;
            const success = await this.mediaControlCallback(this.getDeviceId(), control);
            if (success) {
                request.responseValue.control = control;
            }
            return success;
        }
    };
}
//# sourceMappingURL=MediaController.js.map