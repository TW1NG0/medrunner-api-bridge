"use strict";
/**
 * StartStopController - Adds start/stop control capability
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.StartStopController = StartStopController;
const EventLimiter_1 = require("../core/EventLimiter");
const types_1 = require("../core/types");
function StartStopController(Base) {
    return class extends Base {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        constructor(...args) {
            super(...args);
            this.startStopCallback = null;
            this.startStopEventLimiter = new EventLimiter_1.EventLimiter(types_1.EVENT_LIMIT_STATE);
            this.registerRequestHandler(this.handleStartStopRequest.bind(this));
        }
        onStartStop(callback) {
            this.startStopCallback = callback;
        }
        async sendStartStopEvent(start, cause = types_1.PHYSICAL_INTERACTION) {
            if (this.startStopEventLimiter.isLimited()) {
                return false;
            }
            return this.sendEvent('setStartStop', { state: start }, cause);
        }
        async handleStartStopRequest(request) {
            if (request.action !== 'setStartStop' || !this.startStopCallback) {
                return false;
            }
            const start = request.requestValue.state;
            const result = await this.startStopCallback(this.getDeviceId(), start);
            // Handle both boolean and object return types
            let success;
            if (typeof result === 'boolean') {
                success = result;
            }
            else {
                success = result.success;
                if (result.message) {
                    request.errorMessage = result.message;
                }
            }
            if (success) {
                request.responseValue.state = start;
            }
            return success;
        }
    };
}
//# sourceMappingURL=StartStopController.js.map