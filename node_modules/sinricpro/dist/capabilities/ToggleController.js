"use strict";
/**
 * ToggleController - Adds generic toggle control capability
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ToggleController = ToggleController;
const EventLimiter_1 = require("../core/EventLimiter");
const types_1 = require("../core/types");
function ToggleController(Base) {
    return class extends Base {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        constructor(...args) {
            super(...args);
            this.toggleStateCallback = null;
            this.toggleEventLimiter = new EventLimiter_1.EventLimiter(types_1.EVENT_LIMIT_STATE);
            this.registerRequestHandler(this.handleToggleRequest.bind(this));
        }
        onToggleState(callback) {
            this.toggleStateCallback = callback;
        }
        async sendToggleStateEvent(state, cause = types_1.PHYSICAL_INTERACTION) {
            if (this.toggleEventLimiter.isLimited()) {
                return false;
            }
            return this.sendEvent('setToggleState', { state }, cause);
        }
        async handleToggleRequest(request) {
            if (request.action !== 'setToggleState' || !this.toggleStateCallback) {
                return false;
            }
            const state = request.requestValue.state;
            const result = await this.toggleStateCallback(this.getDeviceId(), state);
            // Handle both boolean and object return types
            let success;
            if (typeof result === 'boolean') {
                success = result;
            }
            else {
                success = result.success;
                if (result.message) {
                    request.errorMessage = result.message;
                }
            }
            if (success) {
                request.responseValue.state = state;
            }
            return success;
        }
    };
}
//# sourceMappingURL=ToggleController.js.map