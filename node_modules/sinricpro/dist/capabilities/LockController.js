"use strict";
/**
 * LockController - Adds lock/unlock capability
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.LockController = LockController;
const EventLimiter_1 = require("../core/EventLimiter");
const types_1 = require("../core/types");
function LockController(Base) {
    return class extends Base {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        constructor(...args) {
            super(...args);
            this.lockStateCallback = null;
            this.lockEventLimiter = new EventLimiter_1.EventLimiter(types_1.EVENT_LIMIT_STATE);
            this.registerRequestHandler(this.handleLockRequest.bind(this));
        }
        onLockState(callback) {
            this.lockStateCallback = callback;
        }
        async sendLockStateEvent(lockState, cause = types_1.PHYSICAL_INTERACTION) {
            if (this.lockEventLimiter.isLimited()) {
                return false;
            }
            return this.sendEvent('setLockState', { state: lockState }, cause);
        }
        async handleLockRequest(request) {
            if (request.action !== 'setLockState' || !this.lockStateCallback) {
                return false;
            }
            const lockState = request.requestValue.state;
            const success = await this.lockStateCallback(this.getDeviceId(), lockState);
            if (success) {
                request.responseValue.state = lockState;
            }
            return success;
        }
    };
}
//# sourceMappingURL=LockController.js.map