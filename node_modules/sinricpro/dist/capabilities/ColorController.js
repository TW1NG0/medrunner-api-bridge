"use strict";
/**
 * ColorController - Adds RGB color control capability
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ColorController = ColorController;
const EventLimiter_1 = require("../core/EventLimiter");
const types_1 = require("../core/types");
function ColorController(Base) {
    return class extends Base {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        constructor(...args) {
            super(...args);
            this.colorCallback = null;
            this.colorEventLimiter = new EventLimiter_1.EventLimiter(types_1.EVENT_LIMIT_STATE);
            this.registerRequestHandler(this.handleColorRequest.bind(this));
        }
        onColor(callback) {
            this.colorCallback = callback;
        }
        async sendColorEvent(r, g, b, cause = types_1.PHYSICAL_INTERACTION) {
            if (this.colorEventLimiter.isLimited()) {
                return false;
            }
            return this.sendEvent('setColor', {
                color: { r, g, b },
            }, cause);
        }
        async handleColorRequest(request) {
            if (request.action !== 'setColor' || !this.colorCallback) {
                return false;
            }
            const { r, g, b } = request.requestValue.color;
            const result = await this.colorCallback(this.getDeviceId(), r, g, b);
            // Handle both boolean and object return types
            let success;
            if (typeof result === 'boolean') {
                success = result;
            }
            else {
                success = result.success;
                if (result.message) {
                    request.errorMessage = result.message;
                }
            }
            if (success) {
                request.responseValue.color = { r, g, b };
            }
            return success;
        }
    };
}
//# sourceMappingURL=ColorController.js.map